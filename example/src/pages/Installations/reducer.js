const INITIAL_STATE = { name2: 'sample page state' };

const Reducer = (state = INITIAL_STATE, action) => {
    switch (action.type) {
        default:
            return state;
    }
};

export default Reducer;
